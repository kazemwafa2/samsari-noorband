// این تابع قبلا فقط console.log("backup") می‌زد — یعنی اسمش «بکاپ
// دیتابیس» بود ولی هیچ بکاپی نمی‌گرفت. بکاپ واقعی دیتابیس کاری نیست
// که از کد کلاینت/سرور Next.js به‌طور معناداری قابل انجام باشد؛ راه
// درست یکی از این‌هاست:
//
// 1) Supabase → Settings → Database → Backups: در پلن‌های پولی
//    Supabase، بکاپ روزانه خودکار داخل خود پلتفرم انجام می‌شود
//    (ساده‌ترین و مطمئن‌ترین راه).
// 2) یک GitHub Action / cron خارجی که هر شب با pg_dump یک نسخه از
//    دیتابیس را در جایی امن (S3, Google Drive, ...) ذخیره کند.
//
// این فایل عمدا دیگر ادعای انجام بکاپ نمی‌کند.
export function backupDatabase() {
  console.warn(
    "backupDatabase() فقط یک placeholder است و بکاپ واقعی نمی‌گیرد — " +
      "به src/lib/backup.ts مراجعه کن."
  );
}
